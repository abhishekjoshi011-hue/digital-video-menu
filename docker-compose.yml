version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: mongodb_container
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    environment:
      MONGO_INITDB_DATABASE: restaurant_db
    networks:
      - restaurant-network

  backend:
    build: ./backend
    container_name: restaurant_backend
    ports:
      - "8080:8080"
    depends_on:
      - mongodb
    networks:
      - restaurant-network
    environment:
      SPRING_MONGODB_URI: REDACTED_MONGODB_URI
      APP_JWT_SECRET: ZGV2LWxvY2FsLWp3dC1zZWNyZXQtMTIzNDU2Nzg5MDEyMzQ1Njc4OTAxMg==
      APP_QR_SECRET: ZGV2LWxvY2FsLXFyLXNlY3JldC0xMjM0NTY3ODkwMTIzNDU2Nzg5MDEyMw==
      APP_AUTH_SETUP_KEY: local-setup-key
      APP_AUTH_ALLOW_PUBLIC_REGISTRATION: "true"
      APP_SEED_SOY_AFFAIR_ENABLED: ${APP_SEED_SOY_AFFAIR_ENABLED:-false}
      APP_SEED_SOY_AFFAIR_TENANT_ID: ${APP_SEED_SOY_AFFAIR_TENANT_ID:-soy affair}
    entrypoint: ["sh", "-c", "echo $$SPRING_MONGODB_URI && java -jar app.jar"]

  frontend:
    build: ./react
    container_name: restaurant_frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - restaurant-network

volumes:
  mongo-data:

networks:
  restaurant-network:
